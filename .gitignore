# Clones de référence (dépôts tiers, avec leur propre .git) — ne pas versionner.
.references/*/
!.references/MAP.md

# Généré par matugen à chaque `wallset`.
extension/*/stylesheet.css

# Artefact de glib-compile-schemas.
extension/*/schemas/gschemas.compiled
